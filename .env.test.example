ENV=test

# test database configuration (used across all persistence implementations)

DB_PREFIX=postgresql+psycopg
DB_USER=user
DB_PASS=pass
DB_HOST=localhost
DB_PORT=5432
DB_NAME=decision_engine_test
