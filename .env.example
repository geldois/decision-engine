# postgresql for a real database, mem for an ephemeral in-process store
# (mem needs no DATABASE_URL — ideal for a zero-infra demo deploy)
PERSISTENCE=postgresql
DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/decision_engine_dev
PORT=8000
